/**
 * @fileoverview 身体矩形，用于简单判断是否发生碰撞
 * @author meifan@gameley.cn (梅凡)
 */

const BodyRectComponent = require('BodyRectComponent');

const AttackRect = cc.Class({
    extends: BodyRectComponent,

    
});