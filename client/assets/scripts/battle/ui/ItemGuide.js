cc.Class({
    extends: cc.Component,

    properties: {
        tipsLabel: cc.Label,
        showEffect: cc.Animation,
        checkNode: cc.Node,
        checkEffect: cc.Animation,

    },

    init() {

    },

    // update (dt) {},
});